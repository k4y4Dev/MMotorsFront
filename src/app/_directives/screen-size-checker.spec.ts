import { ScreenSizeChecker } from './screen-size-checker';

describe('ScreenSizeChecker', () => {
  it('should create an instance', () => {
    const directive = new ScreenSizeChecker();
    expect(directive).toBeTruthy();
  });
});
